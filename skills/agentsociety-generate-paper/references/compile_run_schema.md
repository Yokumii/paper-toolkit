# paper-toolkit Compile Run Schema

Each compile attempt writes `paper/compile_runs/rN/run.json`. The run contains `id`, `ok`, `pdf_path`, `log_path`, structured LaTeX `errors`, `warnings`, page metadata, `attempt_index`, and `duration_seconds`.
