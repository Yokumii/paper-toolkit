# paper-toolkit Check Report Schema

Check reports contain `checker`, `issues`, `result`, `error_count`, `warning_count`, and `ok`. Issue severities are `error`, `warning`, and `info`. CLI envelopes convert error issues into `errors` and warning issues into `warnings`.
